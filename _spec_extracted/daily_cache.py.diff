--- project/daily_cache.py
+++ spec/daily_cache.py
@@ -30,3 +30,3 @@
 from kiteconnect import KiteConnect
-from config import NIFTY50_TOKEN
+from config import NIFTY50_TOKEN, today_ist
 
@@ -119,5 +119,6 @@
 
-        self._loaded = loaded > 0
+        self._loaded = loaded >= max(1, int(len(universe) * 0.8))
         print(f"[DailyCache] Loaded {loaded}/{len(universe)} tokens. "
-              f"Failed: {failed}")
+              f"Failed: {failed}. "
+              f"Cache ready: {self._loaded}")
 
@@ -208,5 +209,5 @@
     def _fetch_daily(self, token: int, days: int = 75) -> list:
-        from_dt = datetime.date.today() - datetime.timedelta(days=days)
+        from_dt = today_ist() - datetime.timedelta(days=days)
         return self.kite.historical_data(
-            token, from_dt, datetime.date.today(), "day"
+            token, from_dt, today_ist(), "day"
         )
